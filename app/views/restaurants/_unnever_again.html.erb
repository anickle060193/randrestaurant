<%= form_for current_user.never_agains.find_by( restaurant: @restaurant ), method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :restaurant_id, @restaurant.id %>
  <%= button_tag "Unnever Again", type: "submit", class: "btn btn-link" %>
<% end %>