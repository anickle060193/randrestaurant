<%= form_for current_user.never_agains.new, remote: true do |f| %>
  <%= hidden_field_tag :place_id, @restaurant.place_id %>
  <%= button_tag "Never Again", type: "submit", class: "btn btn-danger" %>
<% end %>