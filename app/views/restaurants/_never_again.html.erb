<%= form_for current_user.never_agains.new, remote: true do |f| %>
  <%= hidden_field_tag :restaurant_id, @restaurant.id %>
  <%= button_tag "Never Again", type: "submit", class: "btn btn-link" %>
<% end %>